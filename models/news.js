export default class News {
    constructor(id, writer, category, outline, details) {
        this.id = id,
        this.writer = writer,
        this.category = category,
        this.outline = outline,
        this.details = details
    }
}